# Phase 6 - Backend: Notificações e Jobs (Celery + Email)

**Phase:** 06-notifications-deploy
**Plan:** 06-01 (backend)
**Goal:** Implementar lembretes semanais por email e alertas de média histórica usando Celery + Redis
**Depends on:** Phase 5 (Budget Goals) — endpoints /api/metas/ existentes
**CONTEXT:** 06-CONTEXT.md
**RESEARCH:** 06-RESEARCH.md

---

## Wave 1: Setup Celery e Redis

### Task 1.1 - Instalar dependências

**Files modified:** `requirements.txt`

**Action:**

1. Adicionar ao `requirements.txt`:
   ```
   celery[redis]>=5.3.0
   redis>=5.0.0
   psycopg2-binary>=2.9.0
   dj-database-url>=2.1.0
   ```

2. Instalar localmente:
   ```bash
   pip install celery[redis] redis psycopg2-binary dj-database-url
   ```

**Acceptance criteria:**
- [ ] `requirements.txt` contém todas as dependências novas
- [ ] `pip install -r requirements.txt` completa sem erros

---

### Task 1.2 - Configurar Celery no Django

**Files modified:** `backend/celery.py` (new), `backend/__init__.py`, `backend/settings.py`

**Action:**

1. Criar `backend/celery.py`:
   ```python
   import os
   from celery import Celery

   os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'backend.settings')
   app = Celery('backend')
   app.config_from_object('django.conf:settings', namespace='CELERY')
   app.autodiscover_tasks()
   ```

2. Modificar `backend/__init__.py`:
   ```python
   from .celery import app as celery_app
   __all__ = ('celery_app',)
   ```

3. Adicionar ao `backend/settings.py`:
   ```python
   # Celery Config
   CELERY_BROKER_URL = os.environ.get('REDIS_URL', 'redis://localhost:6379/0')
   CELERY_RESULT_BACKEND = os.environ.get('REDIS_URL', 'redis://localhost:6379/0')
   CELERY_ACCEPT_CONTENT = ['json']
   CELERY_TASK_SERIALIZER = 'json'
   CELERY_RESULT_SERIALIZER = 'json'
   CELERY_TIMEZONE = 'America/Sao_Paulo'
   CELERY_BEAT_SCHEDULE = {
       'weekly-reminder': {
           'task': 'api.tasks.send_weekly_reminder',
           'schedule': crontab(day_of_week=1, hour=9, minute=0),  # Segunda 9h
       },
       'monthly-average-alert': {
           'task': 'api.tasks.check_monthly_average',
           'schedule': crontab(day_of_week=1, hour=10, minute=0),  # Segunda 10h
       },
   }
   ```
   Não esquecer o import: `from celery.schedules import crontab`

**Acceptance criteria:**
- [ ] `celery.py` existe e configura o app corretamente
- [ ] `__init__.py` importa o celery app no startup do Django
- [ ] `settings.py` tem CELERY_BROKER_URL, CELERY_RESULT_BACKEND e CELERY_BEAT_SCHEDULE
- [ ] `python -c "from backend.celery import app; print(app)"` funciona

---

## Wave 2: Configuração de Email

### Task 2.1 - Email backend no Django

**Files modified:** `backend/settings.py`

**Action:**

1. Adicionar configuração de email ao `settings.py`:
   ```python
   # Email Config
   EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
   EMAIL_HOST = os.environ.get('EMAIL_HOST', 'smtp.sendgrid.net')
   EMAIL_PORT = int(os.environ.get('EMAIL_PORT', '587'))
   EMAIL_USE_TLS = os.environ.get('EMAIL_USE_TLS', 'True').lower() == 'true'
   EMAIL_HOST_USER = os.environ.get('EMAIL_HOST_USER', 'apikey')
   EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_HOST_PASSWORD', '')
   DEFAULT_FROM_EMAIL = os.environ.get('DEFAULT_FROM_EMAIL', 'Sem Aperreio <noreply@semaperreio.app>')
   ```

2. Para desenvolvimento local, usar console backend se `EMAIL_HOST_PASSWORD` não estiver setado:
   ```python
   if not EMAIL_HOST_PASSWORD and DEBUG:
       EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
   ```

**Acceptance criteria:**
- [ ] Configuração de email existe em `settings.py`
- [ ] Usa variáveis de ambiente para credenciais
- [ ] Fallback para console backend em dev sem senha

---

## Wave 3: Tasks do Celery

### Task 3.1 - Criar arquivo de tasks

**Files modified:** `api/tasks.py` (new)

**Action:**

1. Criar `api/tasks.py`:
   ```python
   from celery import shared_task
   from django.core.mail import send_mail
   from django.contrib.auth.models import User
   from django.db.models import Sum
   from django.utils import timezone
   from .models import Gasto, MetaGasto
   from datetime import timedelta
   import calendar

   @shared_task
   def send_weekly_reminder():
       """
       Envia email semanal para usuários que registraram gastos na semana anterior.
       Resumo: total gasto, categoria mais gasta, meta mais próxima do limite.
       """
       # Implementação: iterar usuários ativos, calcular resumo da semana anterior
       # Enviar email com resumo
       pass

   @shared_task
   def check_monthly_average():
       """
       Verifica se gasto do mês atual ultrapassa média dos últimos 6 meses + 20%.
       Envia email de alerta se sim.
       """
       # Implementação: para cada usuário com gastos nos últimos 7 meses,
       # calcular média dos últimos 6 meses (excluindo atual)
       # comparar com gasto do mês atual
       # enviar email se ultrapassar 120% da média
       pass
   ```

**Acceptance criteria:**
- [ ] `api/tasks.py` existe com ambas as tasks decoradas com `@shared_task`
- [ ] Django carrega as tasks (verificar via `celery -A backend worker --loglevel=info`)

---

### Task 3.2 - Implementar lembrete semanal (NOTF-01)

**Files modified:** `api/tasks.py`, `api/templates/emails/weekly_reminder.html` (new)

**Action:**

1. Completar `send_weekly_reminder()`:
   - Determinar semana anterior (segunda a domingo)
   - Para cada usuário que teve gastos na semana:
     - Calcular total gasto na semana
     - Identificar categoria mais gasta
     - Verificar se há meta próxima do limite (> 80%)
   - Renderizar email com template HTML simples
   - Enviar via `send_mail()`

2. Criar template HTML simples `api/templates/emails/weekly_reminder.html`:
   - Título: "Resumo Semanal — Sem Aperreio"
   - Total gasto na semana
   - Categoria mais gasta
   - Alerta de meta (se houver)
   - Link CTA: "Registrar novo gasto →"

3. Template text fallback para clientes que não renderizam HTML

**Acceptance criteria:**
- [ ] Task `send_weekly_reminder` calcula resumo da semana anterior corretamente
- [ ] Email é enviado apenas para usuários com gastos na semana
- [ ] Template HTML renderiza total, categoria top e alerta de meta
- [ ] Link CTA aponta para a URL do app (variável de ambiente `FRONTEND_URL`)
- [ ] Fallback text/plain existe

---

### Task 3.3 - Implementar alerta de média histórica (NOTF-02)

**Files modified:** `api/tasks.py`, `api/templates/emails/average_alert.html` (new)

**Action:**

1. Completar `check_monthly_average()`:
   - Para cada usuário com histórico de gastos:
     - Mês atual: sum de gastos do mês atual
     - Média: sum de gastos dos últimos 6 meses completos / 6
     - Se mês atual > média * 1.2:
       - Calcular porcentagem acima da média
       - Identificar categoria(s) que mais contribuíram
       - Enviar email de alerta

2. Criar template `api/templates/emails/average_alert.html`:
   - Título: "Alerta de Gastos — Sem Aperreio"
   - "Seu gasto este mês está {pct}% acima da média histórica"
   - Valores: "R$ {atual} vs média R$ {media}"
   - Categoria(s) que mais subiram
   - Dica: "Acesse o dashboard para ver detalhes →"

3. Evitar spam: registrar no banco (ou Redis) último alerta enviado por usuário/mês. Não enviar mais de 1 alerta por mês.

**Acceptance criteria:**
- [ ] Cálculo da média dos últimos 6 meses está correto (exclui mês atual)
- [ ] Alerta só dispara quando gasto > média * 1.2
- [ ] Email contém valores atual, média, e categoria(s) problemática(s)
- [ ] Máximo 1 alerta por usuário por mês
- [ ] Usuário sem histórico de 6 meses não recebe alerta (média insuficiente)

---

## Wave 4: Templates de Email e UI

### Task 4.1 - Templates de email com estilo consistente

**Files modified:** `api/templates/emails/base_email.html` (new)

**Action:**

1. Criar template base `api/templates/emails/base_email.html`:
   - Layout simples, mobile-friendly
   - Cores: fundo escuro (#0f172a), acentos verdes (#22c55e)
   - Logo/nome "Sem Aperreio" no topo
   - Footer com "Você recebeu este email porque tem uma conta no Sem Aperreio"

2. Refatorar `weekly_reminder.html` e `average_alert.html` para estender o base

**Acceptance criteria:**
- [ ] Template base existe com layout responsivo
- [ ] Cores consistentes com o app (tema escuro + verde)
- [ ] Todos os emails herdam do template base

---

### Task 3.4 - Endpoint de trigger para tasks (fallback sem Celery worker)

**Files modified:** `api/views.py`, `api/urls.py`

**Action:**

1. Adicionar view `trigger_tasks(request)`:
   ```python
   @api_view(['POST'])
   @permission_classes([AllowAny])
   def trigger_tasks(request):
       """
       Endpoint para acionar tasks manualmente.
       Protegido por secret key para evitar spam.
       Usado por cron-job.org quando Railway free tier não suporta worker.
       """
       secret = request.data.get('secret') or request.GET.get('secret')
       expected = os.environ.get('TASK_TRIGGER_SECRET', '')
       if not expected or secret != expected:
           return Response({'erro': 'Unauthorized'}, status=403)
       
       task_type = request.data.get('task') or request.GET.get('task', 'all')
       results = {}
       
       if task_type in ('all', 'reminder'):
           results['reminder'] = send_weekly_reminder()
       if task_type in ('all', 'average'):
           results['average'] = check_monthly_average()
       
       return Response({'status': 'ok', 'results': results})
   ```

2. Registrar URL: `path('tasks/trigger/', views.trigger_tasks)` (ou POST para proteger melhor)

3. Documentar: variável `TASK_TRIGGER_SECRET` deve ser configurada no Railway e no cron-job.org.

**Acceptance criteria:**
- [ ] Endpoint aceita POST com `secret` e `task` (reminder|average|all)
- [ ] Retorna 403 se secret incorreto ou não configurado
- [ ] Executa as tasks síncronamente e retorna resultado
- [ ] Documentado como fallback para quando Celery worker não está rodando

---

## Wave 5: Endpoint de Teste / Status

### Task 5.1 - Endpoint para verificar status de notificações

**Files modified:** `api/views.py`, `api/urls.py`

**Action:**

1. Adicionar view `notificacoes_status(request)`:
   ```python
   @api_view(['GET'])
   @permission_classes([IsAuthenticated])
   def notificacoes_status(request):
       """Retorna configuração de notificações do usuário logado."""
       return Response({
           'email_configurado': bool(settings.EMAIL_HOST_PASSWORD),
           'lembrete_semanal': True,  # futuro: toggle por usuário
           'alerta_media': True,
       })
   ```

2. Registrar URL: `path('notificacoes/status/', views.notificacoes_status)`

**Acceptance criteria:**
- [ ] GET /api/notificacoes/status/ retorna configuração de notificações
- [ ] Requer autenticação
- [ ] Email configurado reflete se `EMAIL_HOST_PASSWORD` está setado

---

## Verification

- [ ] `celery -A backend worker --loglevel=info` inicia sem erros
- [ ] `celery -A backend beat --loglevel=info` inicia sem erros
- [ ] Task `send_weekly_reminder` executa manualmente (`delay()` ou `apply_async()`)
- [ ] Task `check_monthly_average` executa manualmente
- [ ] Email enviado com template HTML correto (console backend em dev)
- [ ] Cálculo de média histórica está correto
- [ ] Duplicata de alerta por mês é evitada
- [ ] Endpoint `/api/notificacoes/status/` funciona
- [ ] Variáveis de ambiente documentadas (EMAIL_HOST_PASSWORD, REDIS_URL, TASK_TRIGGER_SECRET, etc.)

