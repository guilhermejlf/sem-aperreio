# Phase 8 - Plan 08-02: Frontend Auth Improvements

**Phase:** 08-auth-polish
**Plan:** 08-02 (frontend)
**Depends on:** 08-01 (backend endpoints)
**Wave:** 2

---

## Files Modified

- `frontend/src/components/AuthView.vue` — Add forgot password link, visual improvements
- `frontend/src/components/LoginForm.vue` — Support email/username, show errors
- `frontend/src/components/RegisterForm.vue` — Password strength, confirm password
- `frontend/src/components/ForgotPasswordModal.vue` — NEW — password reset flow
- `frontend/src/components/PasswordResetView.vue` — NEW — reset password page
- `frontend/src/components/VerifyEmailView.vue` — NEW — email verification page
- `frontend/src/config/api.js` — New auth endpoints

---

## Tasks

### Task 1: Update api.js with new endpoints

```xml
<task>
<id>t1</id>
<name>Update API endpoints</name>
<action>
Add to frontend/src/config/api.js:
- AUTH_PASSWORD_RESET: '/api/auth/password-reset/'
- AUTH_PASSWORD_RESET_CONFIRM: '/api/auth/password-reset/confirm/'
- AUTH_VERIFY_EMAIL: '/api/auth/verify-email/'
</action>
<read_first>
- frontend/src/config/api.js
</read_first>
<acceptance_criteria>
- api.js contains new AUTH endpoints
</acceptance_criteria>
</task>
```

### Task 2: Update LoginForm for email/username

```xml
<task>
<id>t2</id>
<name>Update LoginForm</name>
<action>
Update frontend/src/components/LoginForm.vue:
1. Change label from "Usuário" to "Usuário ou Email"
2. Update placeholder to "seu_usuario ou email@exemplo.com"
3. Send field as "identifier" instead of "username"
4. Handle 403 response for unverified email
5. Improve error messages (amigáveis em português)
6. Add loading state visual feedback
</action>
<read_first>
- frontend/src/components/LoginForm.vue
- frontend/src/config/api.js
</read_first>
<acceptance_criteria>
- LoginForm label says "Usuário ou Email"
- Sends "identifier" field
- Handles 403 with unverified message
- Loading state on button
</acceptance_criteria>
</task>
```

### Task 3: Update RegisterForm with password strength

```xml
<task>
<id>t3</id>
<name>Update RegisterForm</name>
<action>
Update frontend/src/components/RegisterForm.vue:
1. Add password2 field ("Confirmar senha")
2. Add real-time password strength indicator (barra ou checklist)
3. Validate password matches password2
4. Show specific error for weak password
5. Show success message: "Verifique seu email para ativar sua conta"
6. Prevent login redirect after register (wait for verification)
</action>
<read_first>
- frontend/src/components/RegisterForm.vue
</read_first>
<acceptance_criteria>
- RegisterForm has password2 field
- Password strength indicator visible
- Validates password match
- Shows verification message after register
</acceptance_criteria>
</task>
```

### Task 4: Add forgot password link

```xml
<task>
<id>t4</id>
<name>Add forgot password link to AuthView</name>
<action>
Update frontend/src/components/AuthView.vue:
1. Add "Esqueci minha senha" link below login form
2. Click opens ForgotPasswordModal
3. Keep existing tab structure
</action>
<read_first>
- frontend/src/components/AuthView.vue
- frontend/src/components/LoginForm.vue
</read_first>
<acceptance_criteria>
- AuthView has forgot password link
- Link opens modal or route
</acceptance_criteria>
</task>
```

### Task 5: Create ForgotPasswordModal

```xml
<task>
<id>t5</id>
<name>Create ForgotPasswordModal</name>
<action>
Create frontend/src/components/ForgotPasswordModal.vue:
1. Input field for email
2. Submit button "Enviar link"
3. Call AUTH_PASSWORD_RESET endpoint
4. Show success: "Se o email existir, você receberá um link"
5. Show error if email invalid format
6. Loading state
</action>
<read_first>
- frontend/src/components/AuthView.vue
- frontend/src/config/api.js
</read_first>
<acceptance_criteria>
- ForgotPasswordModal.vue exists
- Has email input and submit
- Calls password reset endpoint
- Shows appropriate messages
</acceptance_criteria>
</task>
```

### Task 6: Create PasswordResetView

```xml
<task>
<id>t6</id>
<name>Create PasswordResetView</name>
<action>
Create frontend/src/components/PasswordResetView.vue:
1. Route: /reset-password?token=xxx
2. Validate token presence
3. Two password fields (new + confirm)
4. Strength indicator
5. Submit to AUTH_PASSWORD_RESET_CONFIRM
6. Show success → redirect to login after 3s
7. Show error for invalid token
</action>
<read_first>
- frontend/src/App.vue (to see routing)
- frontend/src/config/api.js
</read_first>
<acceptance_criteria>
- PasswordResetView.vue exists
- Reads token from URL
- Has password + confirm fields
- Calls reset confirm endpoint
- Redirects to login on success
</acceptance_criteria>
</task>
```

### Task 7: Create VerifyEmailView

```xml
<task>
<id>t7</id>
<name>Create VerifyEmailView</name>
<action>
Create frontend/src/components/VerifyEmailView.vue:
1. Route: /verify-email?token=xxx
2. Call AUTH_VERIFY_EMAIL endpoint on mount
3. Show loading state
4. Show success: "Email confirmado! Você já pode fazer login."
5. Show error for invalid/expired token
6. Link to login page
</action>
<read_first>
- frontend/src/App.vue (routing)
- frontend/src/config/api.js
</read_first>
<acceptance_criteria>
- VerifyEmailView.vue exists
- Reads token from URL
- Calls verify endpoint on mount
- Shows success/error states
- Has link to login
</acceptance_criteria>
</task>
```

### Task 8: Add routes

```xml
<task>
<id>t8</id>
<name>Add auth routes</name>
<action>
Update frontend routing (App.vue or router):
- /reset-password → PasswordResetView
- /verify-email → VerifyEmailView
</action>
<read_first>
- frontend/src/App.vue
</read_first>
<acceptance_criteria>
- Routes exist for reset-password and verify-email
</acceptance_criteria>
</task>
```

### Task 9: Visual polish on AuthView

```xml
<task>
<id>t9</id>
<name>Polish AuthView visuals</name>
<action>
Update frontend/src/components/AuthView.vue:
1. Better error/success message styling (toast-like or inline with icons)
2. Loading spinner on buttons
3. Input focus states consistent with app theme
4. Add small info text: "Precisa de ajuda?" with contact/faq link (optional)
5. Ensure mobile responsiveness
</action>
<read_first>
- frontend/src/components/AuthView.vue
</read_first>
<acceptance_criteria>
- Error messages styled with icon/color
- Loading states visible
- Mobile responsive
</acceptance_criteria>
</task>
```

---

## Verification

- [x] Login accepts username or email
- [x] Register shows password strength
- [x] Register requires password confirmation
- [x] "Esqueci minha senha" opens modal
- [x] Password reset email sent from modal
- [x] Reset password page works with token
- [x] Email verification page confirms account
- [x] Error messages are amigáveis
- [x] All forms have loading states

## Must Haves

1. Login field accepts username or email
2. Password strength visible during register
3. Password reset flow complete (request + reset)
4. Email verification flow complete
5. Visual feedback on all actions
