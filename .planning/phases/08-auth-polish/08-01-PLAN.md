# Phase 8 - Plan 08-01: Backend Auth Improvements

**Phase:** 08-auth-polish
**Plan:** 08-01 (backend)
**Depends on:** None (extends existing auth)
**Wave:** 1

---

## Files Modified

- `api/models.py` — Add UserProfile model (email_verified, verification_token, reset_token, reset_token_expires)
- `api/views_auth.py` — Add password reset, email verification, login by email endpoints
- `api/serializers_auth.py` — Add password validation, confirm password
- `api/urls.py` — New auth endpoints
- `api/tasks.py` — Email sending for verification and reset
- `backend/settings/base.py` — Email templates config (if needed)

---

## Tasks

### Task 1: Create UserProfile model

```xml
<task>
<id>t1</id>
<name>Create UserProfile model</name>
<action>
Add UserProfile model to api/models.py with fields:
- user: OneToOneField(User)
- email_verified: BooleanField(default=False)
- verification_token: CharField(max_length=64, blank=True, null=True)
- verification_token_expires: DateTimeField(blank=True, null=True)
- reset_token: CharField(max_length=64, blank=True, null=True)
- reset_token_expires: DateTimeField(blank=True, null=True)

Create migration and apply it.
</action>
<read_first>
- api/models.py
</read_first>
<acceptance_criteria>
- api/models.py contains class UserProfile
- UserProfile has all required fields
- Migration file exists and has been applied
</acceptance_criteria>
</task>
```

### Task 2: Update RegisterSerializer with validation

```xml
<task>
<id>t2</id>
<name>Update RegisterSerializer</name>
<action>
Update api/serializers_auth.py:
1. Add password validation (min 8 chars, 1 uppercase, 1 lowercase, 1 number, 1 special)
2. Add password2 field for confirmation
3. Validate password1 == password2
4. On create, generate verification_token and send email
5. Create UserProfile with email_verified=False
6. Return message asking user to check email
</action>
<read_first>
- api/serializers_auth.py
- api/models.py
</read_first>
<acceptance_criteria>
- api/serializers_auth.py contains validate_password method
- validate_password checks all 4 rules
- password2 field exists and is validated
- UserProfile created on register with email_verified=False
</acceptance_criteria>
</task>
```

### Task 3: Update LoginView for email/username login

```xml
<task>
<id>t3</id>
<name>Update LoginView for email/username</name>
<action>
In api/views_auth.py, override LoginView.post:
1. Accept 'identifier' field (can be username or email)
2. If identifier contains '@', look up user by email
3. Otherwise look up by username
4. If user found, authenticate with password
5. If email_verified is False, return 403 with message "Confirme seu email antes de entrar"
6. Generate and return JWT tokens
</action>
<read_first>
- api/views_auth.py
- api/models.py
</read_first>
<acceptance_criteria>
- api/views_auth.py LoginView accepts identifier field
- Login with username works
- Login with email works
- Unverified email returns 403 with specific message
</acceptance_criteria>
</task>
```

### Task 4: Email verification endpoint

```xml
<task>
<id>t4</id>
<name>Email verification endpoint</name>
<action>
Add VerifyEmailView to api/views_auth.py:
1. GET /api/auth/verify-email/?token=xxx
2. Find UserProfile by verification_token
3. Check token not expired
4. Set email_verified=True
5. Clear verification_token
6. Return success message
7. If token invalid/expired, return 400
</action>
<read_first>
- api/views_auth.py
- api/models.py
</read_first>
<acceptance_criteria>
- VerifyEmailView exists in api/views_auth.py
- Endpoint handles token validation
- email_verified set to True on success
- Returns 400 for invalid/expired token
</acceptance_criteria>
</task>
```

### Task 5: Password reset request endpoint

```xml
<task>
<id>t5</id>
<name>Password reset request endpoint</name>
<action>
Add PasswordResetRequestView to api/views_auth.py:
1. POST /api/auth/password-reset/
2. Accept email in request body
3. Find user by email
4. Generate reset_token and reset_token_expires (24h)
5. Send email with reset link
6. Return generic message (don't leak if email exists)
</action>
<read_first>
- api/views_auth.py
- api/tasks.py (for email sending patterns)
</read_first>
<acceptance_criteria>
- PasswordResetRequestView exists
- Endpoint accepts email and returns generic message
- reset_token generated and stored
- Email sent with reset link
</acceptance_criteria>
</task>
```

### Task 6: Password reset confirm endpoint

```xml
<task>
<id>t6</id>
<name>Password reset confirm endpoint</name>
<action>
Add PasswordResetConfirmView to api/views_auth.py:
1. POST /api/auth/password-reset/confirm/
2. Accept token and new_password
3. Find user by reset_token
4. Check token not expired
5. Validate new_password meets strength rules
6. Update user password
7. Clear reset_token
8. Return success message
</action>
<read_first>
- api/views_auth.py
</read_first>
<acceptance_criteria>
- PasswordResetConfirmView exists
- Validates token expiration
- Validates password strength
- Updates password on success
</acceptance_criteria>
</task>
```

### Task 7: Email templates

```xml
<task>
<id>t7</id>
<name>Create email templates</name>
<action>
Create templates in api/templates/emails/:
1. email_verification.html — link with verification token
2. password_reset.html — link with reset token

Use existing base_email.html dark theme as base.
</action>
<read_first>
- api/templates/emails/base_email.html
- api/tasks.py
</read_first>
<acceptance_criteria>
- api/templates/emails/email_verification.html exists
- api/templates/emails/password_reset.html exists
- Both extend base_email.html
</acceptance_criteria>
</task>
```

### Task 8: Update URLs

```xml
<task>
<id>t8</id>
<name>Update auth URLs</name>
<action>
Add to api/urls.py:
- /api/auth/password-reset/ → PasswordResetRequestView
- /api/auth/password-reset/confirm/ → PasswordResetConfirmView
- /api/auth/verify-email/ → VerifyEmailView
</action>
<read_first>
- api/urls.py
</read_first>
<acceptance_criteria>
- api/urls.py contains new auth endpoints
</acceptance_criteria>
</task>
```

---

## Verification

- [x] Register creates unverified user and sends email
- [x] Login with username works
- [x] Login with email works
- [x] Unverified user cannot login (403 error)
- [x] Email verification link activates account
- [x] Password reset email sent
- [x] Password reset link works and updates password
- [x] All endpoints return appropriate status codes

## Must Haves

1. User can register with strong password
2. User receives verification email
3. User can login with username or email
4. Unverified accounts blocked from login
5. Password reset flow works end-to-end
